apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pingpong-helm.fullname" . }}-test-connection"
  labels:
{{ include "pingpong-helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pingpong-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
